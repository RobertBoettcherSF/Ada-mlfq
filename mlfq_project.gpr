project MLFQ_Project is

   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Main use ("main.adb");
   for Exec_Dir use "obj";

   package Compiler is
      -- Standard recommended compilation flags for Ada 2012
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnat2012", "-gnatwa");
   end Compiler;

   package Builder is
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnat2012", "-gnatwa");
   end Builder;

end MLFQ_Project;
